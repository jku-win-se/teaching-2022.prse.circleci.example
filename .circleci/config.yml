version: 2
jobs:
  build:
    
    working_directory: ~/at.jku.se.prse.example.calculator

    docker:
      - image: circleci/openjdk:8-jdk-browsers

    steps:

      - checkout
      
      - run: mvn clean verify    
      
      - store_artifacts:
          path: ~/at.jku.se.prse.example.calculator/target 
      